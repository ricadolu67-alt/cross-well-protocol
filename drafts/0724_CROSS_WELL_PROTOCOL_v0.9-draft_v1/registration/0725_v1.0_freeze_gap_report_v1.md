# v1.0 freeze gap report

Generated: 2026-07-25  
Outcome access: `NO_LAYER_D_OUTCOME_ACCESS`

## Current verified state

- Protocol structural validation: PASS.
- YAML/JSON/CSV strict parsing: PASS.
- Source-only sampling audit: complete.
- Frozen model bundle: 5 ExtraTrees + Ridge + SourceMedian + source feature
  medians; all hashes verified and models reload successfully.
- Archive-specific feature-target semantic rows: 115 rows for 23 currently
  qualified families across the base and extension-v4 contract files.
- Measurement-semantic completeness audit: PASS; every qualified archive
  scope has exact common4 plus ROP, all ambiguity statuses PASS, and no numeric
  ROP was accessed.
- Combined T1b: 36 families assessed, 23 label-blinded qualified.
- Primary field gate: 8 fields with at least 2 qualified families.
- Basin-context gate: 5 basin contexts represented by those primary fields.
- ROP numeric tokens converted, retained, summarized or scored: zero.
- Source-only matched falsification diagnostics: PASS; 25 split and 20
  dependence seed-level paired results generated under fixed configurations.
- F-15 semantic track: retrospective failure analysis only.
- Uncertainty-plan synthetic validation: PASS for all primary, robustness and
  supplementary branches.
- Freeze checklist: 18 PASS, 6 PENDING.

## Blocking issue

The minimum field and basin gates now pass, acquisition is closed, and the
integration-v4 102-file inventory is immutable for v1.0. However, OSF
Registration, v1.0 freeze and numeric outcome unlock must not occur until every
remaining freeze-checklist item passes.

The principal researcher accepted the outcome-custody governance mechanism:
principal custody of the original archives, fail-closed preunlock commands,
and principal-only explicit single-use numeric unlock after OSF and GitHub
freeze evidence. The synthetic sentinel access-control dry run passed and its
logs are hashed; F16 is now `PASS`. No real Layer D archive was opened.

The full synthetic protocol dry run also passed. It generated all ten output
tables with exact frozen headers and exercised MD-weighted loss, duplicate-MD
aggregation, qualified-to-scorable attrition, field aggregation, t intervals,
10,000 field bootstraps, leave-one-field/basin-out, diagnostic-output routing
and claim routing. No real Layer D data were opened.

The machine-readable claim and protocol-deviation rule tests passed. All model
outcome branches, diagnostic scope boundaries, integrity labels and the
mandatory non-causal boundary are covered. D1/D2/D3 disposition is executable,
and the active deviation log no longer contains an example row.

The principal researcher accepted the complete dependence/cohort review
packet. The final preunlock registry contains 36 unique conservative families;
the final cohort manifest retains 23 qualified and 13 held families. Thirteen
fields are represented, of which 8 are primary-qualified across 5 basin
contexts. F06 and F15 are now `PASS`.

Three prespecified pre-unlock acquisition extensions are complete. Extension v1
is stored at:

`../0725_CROSS_WELL_preunlock_acquisition_extension_v1/`

All five archives were acquired on `2026-07-25` before the already confirmed
cutoff `2026-07-25T23:59:59+08:00`. T1a and T1b were completed without ROP
numeric access. Montara H3 ST1 and H2 qualified; Montara H4, Macedon 9 and
Macedon 10 failed the frozen covariate-QC rules. No threshold was relaxed.

Extension v2 is stored at:

`../0725_CROSS_WELL_preunlock_acquisition_extension_v2/`

It assessed Blacktip P1 and a new archive for existing Blacktip P2. T1a
confirmed all archive identities and 335 LAS headers. T1b held both families
because the drilling-mechanics objects exposed CRPM/TRPM but no defensible
surface-RPM curve. These mnemonics were not outcome-selected or remapped.
The field gate therefore remains 7/8.

Extension v3 is stored at:

`../0725_CROSS_WELL_preunlock_acquisition_extension_v3/`

It assessed Scarborough 5 and the conservative Scarborough 4/4A family. T1a
confirmed all three archive hashes and inspected 27 LAS headers without
numeric-array access. In T1b, LWD objects provided MD/ROP but lacked WOB,
surface RPM and SPP, whereas PWD objects provided RPM/SPP/ROP but lacked MD
and WOB. The frozen protocol did not permit post hoc cross-file numerical
alignment. Both families were therefore held, and the field gate remains 7/8.

Extension v4 is stored at:

`../0725_CROSS_WELL_preunlock_acquisition_extension_v4/`

It assessed three independent Prelude Development wells. Initial T1b v1
retained all three as held because exact `Tot Depth` and WOB units `klbs`/`t`
were absent from the restricted mapping table. A global preunlock amendment
was documented in `05c_label_blinded_semantic_mapping_amendment_v1.yaml`.
Thirty prior T1b QC files contained zero matching tokens, so no prior family
decision changed. Under T1b v2, Prelude P3, P4 and P6 all passed the unchanged
thresholds and same-source-object rule. Prelude is therefore the eighth
primary-qualified field; the gate is now 8/8 fields and 5/4 basin contexts.

## Remaining protocol engineering

1. Set the final code commit, single execution command and environment identity.
2. Replace remaining v1.0/external-evidence placeholders only after the
   corresponding artifact exists; do not invent receipt or commit identifiers.
3. Generate the final v1.0 SHA-256 manifest and byte-identical frozen ZIP.
4. Obtain OSF Registration and GitHub signed-release evidence before unlock.
5. Verify outcome inaccessibility immediately before authorized unlock.

## Information/actions needed from the principal researcher

Immediate:

- keep all ROP outcomes inaccessible.

Before v1.0:

- provide the author name, affiliation and optional ORCID for OSF metadata;
- choose a dedicated GitHub repository and configure commit signing;
- approve the exact external submission immediately before OSF/GitHub action.

Zenodo metadata and DOI publication are intentionally deferred until the
confirmatory execution and final reproducibility package are complete.
