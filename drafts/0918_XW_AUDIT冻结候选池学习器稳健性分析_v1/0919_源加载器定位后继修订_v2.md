# B第一层源加载器定位后继修订 v2

2026-09-19。本文件从属于0918_冻结候选池稳健性分析合同_v1.md，与v1一起生效。内容定稿；只有完成下述签名tag核验后才能执行。签名与推送的实际状态由Git记录核验，不以本文自行声明。v1及此前草案和失败记录保持不变。

## 1. 本次执行事实

已实现 `scripts/run_pool_v1.py` 并通过小型实现检查。执行时合同signed tag本地验签通过、合同字节与tag版本一致、19项锁定输入哈希全部通过。当前Python/NumPy/pandas/scikit-learn/joblib版本与冻结模型记录一致。

运行在 `contract_and_input_hashes` 阶段因源loader全项目唯一性检查停止。源队列尚未加载；未重拟合训练路径守卫模型，未拟合新候选，未生成新候选Layer D预测。失败记录保留于 `outputs/0918_执行失败记录_v1.json`，脚本及哈希保留其中。

项目中发现两份同名文件，SHA-256均为：

`98d82fe9236fc2cf0a0570dd2e8b849f712e98c7ce41f0fc34b32cecdef2834a`

- 原定位：`drafts/0731_早期Volve论文归档_v1/work_revision/0718_ten_family_nested_lofo_v1.py`
- 独立复现材料副本：`drafts/0918_XW-AUDIT独立复现_v1/R3_源域重训材料包_v1/work_revision/0718_ten_family_nested_lofo_v1.py`

这不是模型预测一致性守卫失败，也不是源文件内容哈希变化。v1 §3沿用0802的“唯一loader副本”定位；执行脚本沿用0802的项目范围同名文件计数检查，因此触发失败关闭。

## 2. 唯一执行规则修订

后继执行以原定位的精确相对路径及上述完整SHA-256双重绑定源loader。必须存在该文件且哈希匹配；不得从搜索结果自动挑选、回退到复制件或更换路径。

执行前扫描drafts/下所有名为0718_ten_family_nested_lofo_v1.py的文件，将全部路径和完整SHA-256写入执行清单。同哈希副本仅记录，不构成错误；任何同名文件哈希不同、不可读或扫描不能完成，立即失败关闭。其他位置的文件一律不导入、不执行，不能回退代替原路径。该规则适用于所有位置的副本，不限于R3材料包。生产执行始终仅加载原路径且必须满足完整哈希。此修改解除同内容复制件造成的目录计数冲突，同时保留异内容副本的混淆警报。

v1其余内容完全沿用：19项锁定输入、269339源行/10单元/SourceMedian、36779训练行身份和顺序、八候选配置、seed、预处理、训练路径函数、评分函数、1e-10容差、原结果/MD-only守卫、全部输出和判读规则均不变。任何训练路径预测守卫失败仍按v1停止，不能以本修订作为放宽容差或更改实现的许可。

## 3. 后继执行与输出保留

v1的signed tag为xw-audit-learner-pool-contract-v1；本修订的signed tag固定为xw-audit-learner-pool-contract-v2。执行脚本v2须分别核验两个tag的签名，并核验当前v1合同与v1 tag对应文件的SHA-256、当前本文件与v2 tag对应文件的SHA-256均一致；额外记录两份合同完整哈希和两个tag的commit。任一失败均不得加载源数据或开始拟合。签名提交、tag及推送按本次用户明确指令执行，实际结果另记，不在文件中预填成功事实。

后继脚本另存v2，核验v1及后继修订的tag字节与哈希。保留原失败记录，不覆盖或删除。允许继续在合同原定outputs目录新建尚不存在的正式结果文件，但必须逐个拒绝覆盖，且只有已识别的前置失败记录允许预先存在；发现任何既有新候选结果即停止，避免隐式恢复或重复评分。

修订签定后从头核验输入并执行；不复用此次未完成的运行状态。本修改发生在任何源数据加载及新候选预测之前，后续报告和执行清单应如实披露定位守卫失败与后继修订。
